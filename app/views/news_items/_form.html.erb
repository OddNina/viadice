<%= semantic_form_for @news_item, url: myprofile_group_news_item_path(@group, @news_item) do |f| %>

<div class="col-lg-12">
  <% if @news_item.errors.any? %>
    <div class="alert alert-danger">
      <% @news_item.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
      </ul>
    </div>
  <% end %>
</div>
<div class="col-lg-8">  
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">
        Headline:
      </h3>
    </div>
    <div class="panel-body">
      <%= f.input :headline, :label => false %>
    </div>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">
        Inhalt:
      </h3>
    </div>
    <div class="panel-body">
      <%= f.input :content, :label => false %>
    </div>
  </div>
</div>
<div class="col-lg-4">
  <div class="form-group">
    <%= f.action :submit, :as => :button, :label => "Nachricht speichern" %>
  </div>
  <div class="form-group">
    <%= link_to 'Abbrechen', myprofile_group_path(@news_item.group), class: "btn btn-default" %>
  </div>
</div>
<% end %>