<%= form_for(@game_style) do |f| %>
  <% if @game_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_style.errors.count, "error") %> prohibited this game_style from being saved:</h2>

      <ul>
      <% @game_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
