<!DOCTYPE html>
<html>
<head>
  <title>Viadice</title>
  <link href='http://fonts.googleapis.com/css?family=Muli|Bree+Serif' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <div class="navbar navbar-fixed-top" role="navigation">
    <div class="navbar-inner">
      <div class="container">
        <a class="navbar-brand" href="#">
          <div class="brand"><span class="logo"></span></div><div class="nav-serif">viaDice</div>
        </a>
        <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to 'Übersicht', overview_myprofile_path %></li>
          <li><%= link_to 'Gruppen', groups_path %></li>
        </ul>
        <ul class="nav pull-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user"></i> <%= current_user.display_name %></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profil", myprofile_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Abmelden", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        </ul>
        <% else %>
        <ul class="nav pull-right">
          <li><%= link_to "Login", new_user_session_path %></li>
        </ul>
        <% end %>
      </div>
    </div>
  </div>
  <div class="container"> 
    <div class="row">
      <div class="col-lg-4"></div>
      <div class="col-lg-4">
          <% if notice %>
          <p class="notice"><%= notice %></p>
          <% end %>
          <% if alert %>
          <p class="alert"><%= alert %></p>
          <% end %>
          <%= yield %>
      <div class="col-lg-4"></div>
    </div>
  </div>
</body>
</html>
